[build]
  command = "npm run fix:strict-live && npm test && npm run test:archive-hu && npm run test:copy && npm run test:coverage"
  publish = "."

[build.environment]
  NODE_VERSION = "22"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
