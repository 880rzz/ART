name: Fix ART browser regressions
on:
  push:
    branches: [main]
    paths:
      - tools/fix-art-browser-regressions-20260825.mjs
      - .github/workflows/fix-art-browser-regressions-20260825.yml
  workflow_dispatch:
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 22
      - run: node tools/fix-art-browser-regressions-20260825.mjs
      - run: node tools/audit-apple-responsive-contract.mjs
      - name: Commit CSS correction
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add assets/css/site.css
          if git diff --cached --quiet; then
            echo 'No CSS changes.'
          else
            git commit -m 'Fix ART browser regressions in clean design authority'
            git push origin HEAD:main
          fi
