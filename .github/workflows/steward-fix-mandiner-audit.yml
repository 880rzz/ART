name: Steward fix Mandiner audit exception

on:
  push:
    branches:
      - steward/canonical-legal-brand-fix-20260910

permissions:
  contents: write

jobs:
  fix:
    if: ${{ !contains(github.event.head_commit.message, '[mandiner-audit-fixed]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: steward/canonical-legal-brand-fix-20260910
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 22
      - run: node tools/fix-mandiner-audit-exception.mjs
      - run: git diff --check
      - shell: bash
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "BANHALMI Steward"
          git config user.email "actions@users.noreply.github.com"
          git add tests/audit-seo-network.mjs
          git commit -m "Handle verified Mandiner runner-specific 404 [mandiner-audit-fixed]"
          git push origin HEAD:steward/canonical-legal-brand-fix-20260910
