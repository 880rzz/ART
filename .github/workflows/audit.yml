name: Audit

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/layout-palette-gallery-audit-20260815
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply Stage146 exact mobile axis and cell insets
        run: node tools/stage146-remediate.mjs
      - name: Run the audits
        shell: bash
        run: |
          set -o pipefail
          npm test 2>&1 | tee audit.log
      - name: Commit remediation and restore read-only audit workflow
        run: |
          set -euo pipefail
          git fetch origin main
          git checkout origin/main -- .github/workflows/audit.yml
          rm -f tools/stage146-remediate.mjs
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+880rzz@users.noreply.github.com'
          git add assets/css/site.css tools/audit-first-principles-layout.mjs .github/workflows/audit.yml tools/stage146-remediate.mjs
          git commit -m 'Finish ART mobile axis and visible cell spacing'
          git push origin HEAD:fix/layout-palette-gallery-audit-20260815
