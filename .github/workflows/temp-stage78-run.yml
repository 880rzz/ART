name: Temporary Stage 78 runner
on: push
permissions:
  contents: write
jobs:
  apply:
    if: github.actor != 'actions'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/apply-stage78-exhibition-families.mjs
      - run: node scripts/bump-editorial-release-cache.mjs
      - run: rm .github/workflows/temp-stage78-run.yml scripts/apply-stage78-exhibition-families.mjs
      - run: |
          git config user.name "ART Release Bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "feat: give remaining exhibitions distinct editorial families"
      - run: npm test
      - run: git push
