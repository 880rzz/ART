name: One-shot ART Stage153 footer domain centering
on:
  push:
    branches:
      - fix/final-curatorial-footer-menu-20260815
    paths:
      - tools/one-shot-stage153-footer-domain-centering.mjs
      - .github/workflows/one-shot-stage153-footer-domain-centering.yml
permissions:
  contents: write
jobs:
  remediate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node tools/one-shot-stage153-footer-domain-centering.mjs
      - run: npm test
      - name: Commit and self-clean
        run: |
          rm tools/one-shot-stage153-footer-domain-centering.mjs
          rm .github/workflows/one-shot-stage153-footer-domain-centering.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Center footer domain row across full width"
          git push
