name: Temporary Stage 86 life journey
on:
  push:
    branches: [fix/art-stage86-life-journey-density]
permissions:
  contents: write
jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node tools/apply-stage86-life-journey-density.mjs
      - run: rm .github/workflows/temp-stage86-life-journey.yml tools/apply-stage86-life-journey-density.mjs
      - run: |
          git config user.name "BANHALMI Release Bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "feat: simplify life journey presentation"
      - run: npm test
      - run: git push
