name: One-shot ART Stage144B final specificity

on:
  push:
    branches:
      - fix/layout-palette-gallery-audit-20260815

permissions:
  contents: write

jobs:
  remediate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/layout-palette-gallery-audit-20260815
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
      - name: Apply final specificity authority
        run: node tools/one-shot-stage144b-specificity-20260815.mjs
      - name: Run source audits
        run: npm test
      - name: Commit final remediation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Harden ART layout authority against legacy specificity"
          git push origin HEAD:fix/layout-palette-gallery-audit-20260815
