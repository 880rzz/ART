name: Fix ART Stage69 press contrast
on:
  push:
    branches: [main]
    paths:
      - tools/fix-stage69-press-contrast-20260825.mjs
      - .github/workflows/fix-stage69-press-contrast-20260825.yml
  workflow_dispatch:
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 22
      - run: node tools/fix-stage69-press-contrast-20260825.mjs
      - run: npm test
      - name: Commit CSS correction
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add assets/css/site.css
          if git diff --cached --quiet; then
            echo 'No CSS changes.'
          else
            git commit -m 'Fix final ART Stage69 press description contrast'
            git push origin HEAD:main
          fi
