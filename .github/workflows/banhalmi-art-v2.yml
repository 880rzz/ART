name: BANHALMI ART archive rebuild

on:
  workflow_dispatch:
  push:
    branches: [main, elon-audit-2026]
    paths:
      - 'assets/css/archive-system.css'
      - 'scripts/apply-unified-design.mjs'
      - 'tests/**'
      - 'package.json'
      - '.github/workflows/banhalmi-art-v2.yml'

permissions:
  contents: write

concurrency:
  group: banhalmi-art-rebuild-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rebuild-and-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Apply concise narrative and unified editorial design
        run: npm run integrate:unified-design
      - name: Run complete repository audit
        run: npm test
      - name: Run human voice audit
        run: npm run test:copy
      - name: Run archive coverage audit
        run: npm run test:coverage
      - name: Run strict clarity audit
        run: npm run test:elon-clarity
      - name: Commit generated page updates
        run: |
          if [[ -n "$(git status --porcelain -- '*.html')" ]]; then
            git config user.name "BANHALMI ART build bot"
            git config user.email "actions@users.noreply.github.com"
            git add -- '*.html'
            git commit -m "Apply concise domain story and unified editorial rhythm"
            git push
          else
            echo "No generated HTML changes."
          fi

# Merge gate: no page may pass with an oversized type scale, repeated domain explanation,
# missing hero/menu/gallery/exhibition entry, or unverified membership links.