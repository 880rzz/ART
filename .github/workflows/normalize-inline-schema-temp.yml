name: Normalize ART inline schema once

on:
  push:
    branches:
      - __disabled_inline_schema_generator__

permissions:
  contents: write

jobs:
  normalize:
    runs-on: ubuntu-24.04
    steps:
      - name: Generator paused while canonical normalization is prepared
        run: echo "disabled"
