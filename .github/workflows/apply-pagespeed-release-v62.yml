name: Disabled temporary PageSpeed release helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - name: Temporary helper is disabled
        run: echo 'Release propagation is complete; this helper is intentionally read-only.'
