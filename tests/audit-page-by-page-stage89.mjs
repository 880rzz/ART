import fs from'node:fs';const errors=[],report=JSON.parse(fs.readFileSync('data/page-simplification-review.json','utf8')),js=fs.readFileSync('assets/js/responsive-header-system.js','utf8'),css=fs.readFileSync('assets/css/homepage-two-tone-authority.css','utf8');for(const t of['STAGE89-PAGE-BY-PAGE-DENSITY','pageDensityScore','dataset.sectionDensity','dataset.sectionRole'])if(!js.includes(t))errors.push('density metadata runtime missing '+t);for(const t of['[data-page-density=','[data-section-density=','[data-section-role='])if(css.includes(t))errors.push('runtime density metadata must not own final geometry: '+t);for(const t of['STAGE142-UNIVERSAL-APPLE-DESIGN-CONTRACT:START','main>section{--banhalmi-section-surface:var(--art-bg)!important','padding-block:clamp(3.75rem,5.8vw,6rem)!important'])if(!css.includes(t))errors.push('stable geometry contract missing '+t);if(!report.total||report.total<80)errors.push('page review inventory unexpectedly small '+report.total);for(const p of report.pages){if(!fs.existsSync(p.file))errors.push('reviewed page missing '+p.file);if(!['dense','balanced','quiet'].includes(p.profile))errors.push('bad profile '+p.file)}const release=JSON.parse(fs.readFileSync('data/design-release.json','utf8')).release;if(report.release!==release)errors.push('review release mismatch '+report.release+' != '+release);if(errors.length){console.error(errors.join('\n'));process.exit(1)}console.log('Stage89/142 passed for '+report.total+' reviewed pages: density remains metadata only and cannot change final padding, width or typography after first paint.');
